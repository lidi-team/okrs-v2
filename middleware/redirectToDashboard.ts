// import { Context, Middleware } from '@nuxt/types';

// const redirectToDashboardMiddlware: Middleware = ({ redirect, route }: Context, __: Function) => {
//   if (route.path === '/') {
//     return redirect('/dashboard');
//   }
// };

// export default redirectToDashboardMiddlware;
