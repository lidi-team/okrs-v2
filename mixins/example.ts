import { Vue, Component } from 'vue-property-decorator';
@Component
export default class AdminMixin extends Vue {
  // do something
}

// Use it
// import Mixin from "@/mixin/example";
// export default class ABC extends Mixins(Mixin)
